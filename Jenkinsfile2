pipeline {
    agent any 
    tools {
        maven 'M2_HOME'
    }
    stages{
        stage('maven commands'){
            steps{
                sh 'mvn clen compile install package'
            }
        }
    }
}